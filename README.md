# Techstack Vehicle Data Sandbox

This repo contains:

- `stitch-export/`: Google Stitch HTML exports (reference only)
- `web/`: React + Vite + TypeScript static site used as a stable automation fixture

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

